#include "dhs.h"

int main()
{
        // ジ
        init_maps();
        add_word(L"las");
        add_word(L"lo");
        add_word(L"abジolaジr");
        add_word(L"abジoジr");
        add_word(L"le");
        add_word(L"ankudusdb");
        free_maps();
        return 0;
}
